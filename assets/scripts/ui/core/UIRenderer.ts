export class UIRenderer {

    static renderEquipment(data: any) {
        console.log("[UIRenderer] APPLY TO NODE (v0.1 mock)", data);

        // v0.1：禁止真实 UI 操作
        // 下一阶段才允许 node.active / label.string
    }
}