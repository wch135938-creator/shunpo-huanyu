// ============================================================
// CardSaveData — 卡牌存档数据结构（V1 预留）
// 职责：定义单个角色的持久化字段
// 位置：Save 层
// ============================================================

export interface CardSaveData {
  /** 角色配置 ID（对应 cards 配置表） */
  cardId: number;
  /** 当前等级 */
  level: number;
  /** 当前星级 */
  star: number;
  /** 当前经验值 */
  exp: number;
}
